apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: security-feed-web-data
  labels:
    app: security-feed-web
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: "longhorn-rwx"
  resources:
    requests:
      storage: 1Gi
