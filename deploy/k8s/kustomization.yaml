apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - deployment.yaml
  - service.yaml
  - frontend.yaml
  - pvc.yaml
  - configmap.yaml
  - secret.yaml
  - postgres.yaml
  # OpenSearch is optional and off by default (not a current priority). To
  # enable it, uncomment the line below and uncomment OPENSEARCH_URL in
  # configmap.yaml.
  # - opensearch.yaml
