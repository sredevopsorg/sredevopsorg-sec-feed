apiVersion: v1
kind: Service
metadata:
  name: security-feed-web
  labels:
    app: security-feed-web
spec:
  selector:
    app: security-feed-web
    component: feed
  ports:
    - name: http
      port: 80
      targetPort: http
  type: ClusterIP
