# PostgreSQL primary store backing the feed archive.
# Mirrors the docker-compose `postgres` service.
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: security-feed-api-postgres
  labels:
    app: security-feed-api
spec:
  # ReadWriteOnce is preferable for PostgreSQL (single-writer); kept as the
  # cluster's Longhorn RWX storage class for this deployment.
  accessModes:
    - ReadWriteMany
  storageClassName: "longhorn-rwx"
  resources:
    requests:
      storage: 1Gi
---
apiVersion: v1
kind: Service
metadata:
  name: postgres
  labels:
    app: security-feed-api
spec:
  selector:
    app: security-feed-api
    component: postgres
  ports:
    - name: postgres
      port: 5432
      targetPort: postgres
  type: ClusterIP
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: security-feed-api-postgres
  labels:
    app: security-feed-api
    component: postgres
spec:
  replicas: 1
  selector:
    matchLabels:
      app: security-feed-api
      component: postgres
  template:
    metadata:
      labels:
        app: security-feed-api
        component: postgres
    spec:
      securityContext:
        # The postgres image's entrypoint runs as root to chown the data dir
        # before dropping to UID 999, so runAsNonRoot/capabilities are left to
        # the image defaults to avoid breaking first-run init on this PVC.
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: postgres
          image: postgres:16-alpine
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 5432
              name: postgres
          envFrom:
            - secretRef:
                name: security-feed-api-secrets
          readinessProbe:
            exec:
              command: ["pg_isready", "-U", "feed", "-d", "postgres"]
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            exec:
              command: ["pg_isready", "-U", "feed", "-d", "postgres"]
            initialDelaySeconds: 15
            periodSeconds: 20
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
          volumeMounts:
            - name: postgres-data
              mountPath: /var/lib/postgresql/data
      volumes:
        - name: postgres-data
          persistentVolumeClaim:
            claimName: security-feed-api-postgres
