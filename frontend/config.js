// Runtime configuration for the security feed frontend.
//
// `API_BASE_URL` points at the backend API origin. Leave it empty ("") to use
// the same origin as this page (the default when served behind a reverse
// proxy). Set it to an absolute URL such as "https://feed.example.com" when
// the frontend is hosted separately from the API.
window.__API_BASE_URL__ = "";
